import React from "react";

function PatchSequenceFigure() {
  const colors = ["#bfdbfe", "#bbf7d0", "#fde68a", "#fecdd3", "#ddd6fe", "#a5f3fc", "#fed7aa", "#c7d2fe", "#d9f99d"];
  return (
    <figure className="not-prose my-7 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">
      <div className="border-b border-slate-200 bg-slate-50 px-5 py-4"><p className="font-bold text-slate-900">ViT turns a two-dimensional image into a sequence of patch tokens</p><p className="mt-1 text-sm text-slate-600">This 3 × 3 teaching image becomes nine patch tokens. Real models commonly use many more.</p></div>
      <div className="overflow-x-auto p-4 md:p-6"><svg viewBox="0 0 860 340" className="h-auto min-w-[720px] w-full" role="img" aria-labelledby="vit-patches-title vit-patches-desc"><title id="vit-patches-title">Image divided into patches and converted into a token sequence</title><desc id="vit-patches-desc">A three by three image grid is flattened into nine patch cards, each projected into an embedding, with a class token and position embeddings added.</desc><defs><marker id="vit-arrow" markerWidth="8" markerHeight="8" refX="7" refY="4" orient="auto"><path d="M0 0 L8 4 L0 8 Z" fill="#64748b" /></marker></defs><rect width="860" height="340" rx="18" fill="#f8fafc" />{colors.map((color, i) => { const r=Math.floor(i/3), c=i%3; return <g key={i}><rect x={35+c*75} y={55+r*75} width="70" height="70" fill={color} stroke="#475569" strokeWidth="2" /><text x={70+c*75} y={95+r*75} textAnchor="middle" fontSize="14" fontWeight="800" fill="#0f172a">P{i+1}</text></g>;})}<text x="145" y="310" textAnchor="middle" fontSize="13" fontWeight="800" fill="#334155">divide image into fixed patches</text><line x1="275" y1="160" x2="335" y2="160" stroke="#64748b" strokeWidth="3" markerEnd="url(#vit-arrow)" /><g transform="translate(350 75)">{colors.map((color,i)=><g key={i}><rect x={i%5*74} y={Math.floor(i/5)*80} width="62" height="58" rx="9" fill={color} stroke="#475569" /><text x={i%5*74+31} y={Math.floor(i/5)*80+34} textAnchor="middle" fontSize="12" fontWeight="800" fill="#0f172a">P{i+1}</text></g>)}</g><text x="505" y="255" textAnchor="middle" fontSize="13" fontWeight="800" fill="#334155">flatten each patch and project to D numbers</text><line x1="680" y1="160" x2="730" y2="160" stroke="#64748b" strokeWidth="3" markerEnd="url(#vit-arrow)" /><rect x="745" y="75" width="85" height="58" rx="10" fill="#f0abfc" stroke="#a21caf" strokeWidth="2" /><text x="787" y="109" textAnchor="middle" fontSize="12" fontWeight="800" fill="#701a75">[CLS]</text><rect x="745" y="145" width="85" height="58" rx="10" fill="#e2e8f0" stroke="#475569" /><text x="787" y="179" textAnchor="middle" fontSize="12" fontWeight="800" fill="#334155">P1 … P9</text><rect x="745" y="215" width="85" height="58" rx="10" fill="#cffafe" stroke="#0891b2" /><text x="787" y="239" textAnchor="middle" fontSize="11" fontWeight="800" fill="#155e75">+ position</text><text x="787" y="255" textAnchor="middle" fontSize="11" fontWeight="800" fill="#155e75">embedding</text></svg></div>
      <figcaption className="border-t border-slate-100 px-5 py-3 text-sm leading-relaxed text-slate-600">A patch token is not a label such as “wheel.” It is a learned vector representation of raw patch values. The Transformer updates each vector by exchanging information with the other tokens.</figcaption>
    </figure>
  );
}

function PositionFigure() {
  const ordered = ["sky", "sky", "tree", "road", "car", "road"];
  const shuffled = ["road", "tree", "sky", "road", "sky", "car"];
  const style: Record<string, string> = { sky: "bg-sky-200 text-sky-900", tree: "bg-emerald-200 text-emerald-900", road: "bg-slate-300 text-slate-800", car: "bg-orange-200 text-orange-900" };
  return (
    <figure className="not-prose my-7 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm md:p-6"><p className="mb-2 font-bold text-slate-900">Patch content alone does not say where it came from</p><p className="mb-5 text-sm text-slate-600">Without position information, these two sequences contain the same token contents even though their layouts differ.</p><div className="grid gap-5 md:grid-cols-2"><div><p className="mb-2 text-xs font-bold uppercase text-emerald-700">Ordered layout</p><div className="grid grid-cols-3 gap-2">{ordered.map((v,i)=><div key={`${v}-${i}`} className={`rounded-lg p-4 text-center text-xs font-bold ${style[v]}`}>{v}<span className="mt-1 block font-mono text-[10px] opacity-70">pos {i+1}</span></div>)}</div></div><div><p className="mb-2 text-xs font-bold uppercase text-rose-700">Same content, shuffled</p><div className="grid grid-cols-3 gap-2">{shuffled.map((v,i)=><div key={`${v}-${i}`} className={`rounded-lg p-4 text-center text-xs font-bold ${style[v]}`}>{v}<span className="mt-1 block font-mono text-[10px] opacity-70">pos {i+1}</span></div>)}</div></div></div><figcaption className="mt-5 text-sm leading-relaxed text-slate-600">ViT adds a learned position vector to every patch embedding. It can therefore distinguish “sky at the top” from the same sky-coloured patch placed near the road.</figcaption></figure>
  );
}

function AttentionOriginsFigure() {
  const projections = [
    ["Query Q", "X × W_Q", "what this token is looking for", "border-fuchsia-200 bg-fuchsia-50 text-fuchsia-900"],
    ["Key K", "X × W_K", "what each token can be matched on", "border-amber-200 bg-amber-50 text-amber-900"],
    ["Value V", "X × W_V", "the information each token can send", "border-emerald-200 bg-emerald-50 text-emerald-900"],
  ] as const;
  return (
    <figure className="not-prose my-7 rounded-2xl border border-violet-200 bg-violet-50 p-5 shadow-sm md:p-6">
      <p className="font-bold text-violet-950">Where Q, K and V actually come from</p>
      <p className="mt-2 text-sm leading-relaxed text-slate-700">Every patch first has a token vector X. The model owns three learned weight matrices. Multiplying X by each matrix creates three different views of the same token.</p>
      <div className="mt-5 grid items-stretch gap-3 lg:grid-cols-[1fr_auto_2fr]">
        <div className="rounded-xl border border-blue-200 bg-white p-4 text-center"><p className="font-bold text-blue-900">Token X</p><p className="mt-2 text-xs text-slate-600">calculated from patch values, patch-projection weights and position</p></div>
        <div className="flex items-center justify-center text-2xl font-black text-violet-400">→</div>
        <div className="grid gap-3 sm:grid-cols-3">{projections.map(([name, formula, purpose, color]) => <div key={name} className={`rounded-xl border p-4 ${color}`}><p className="font-bold">{name}</p><p className="mt-2 font-mono text-sm">{formula}</p><p className="mt-2 text-xs leading-relaxed text-slate-600">{purpose}</p></div>)}</div>
      </div>
      <div className="mt-5 rounded-xl bg-white p-4 text-sm leading-relaxed text-slate-700"><strong>What is learned?</strong> W_Q, W_K and W_V begin with initialized values and training updates them. Q, K and V are not permanent parameters and are not entered by a developer; the model calculates new ones from the current tokens during every forward pass.</div>
    </figure>
  );
}

function EncoderBlockFigure() {
  const steps = [
    ["token sequence", "patch + position", "#dbeafe", "#2563eb"],
    ["LayerNorm", "stabilize values", "#cffafe", "#0891b2"],
    ["Multi-head attention", "tokens exchange context", "#ede9fe", "#7c3aed"],
    ["Add shortcut", "preserve input", "#fef3c7", "#d97706"],
    ["LayerNorm + MLP", "transform each token", "#dcfce7", "#16a34a"],
    ["Add shortcut", "block output", "#fce7f3", "#db2777"],
  ];
  return (
    <figure className="not-prose my-7 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-200 bg-slate-50 px-5 py-4"><p className="font-bold text-slate-900">One Transformer encoder block updates every token</p></div><div className="overflow-x-auto p-5 md:p-6"><div className="flex min-w-[850px] items-stretch gap-3">{steps.map(([title,detail,bg,border],i)=><React.Fragment key={title}><div className="flex-1 rounded-xl border p-4 text-center" style={{backgroundColor:bg,borderColor:border}}><p className="text-sm font-extrabold text-slate-900">{title}</p><p className="mt-2 text-xs text-slate-600">{detail}</p></div>{i<steps.length-1&&<div className="flex items-center text-xl font-black text-slate-400">→</div>}</React.Fragment>)}</div></div><figcaption className="border-t border-slate-100 px-5 py-3 text-sm leading-relaxed text-slate-600">Attention mixes information across tokens. The MLP—two dense layers with a nonlinearity—processes each token position independently. Residual shortcuts make both sublayers learn changes to their inputs.</figcaption></figure>
  );
}

function AttentionMatrixFigure() {
  const values = [[0.28,0.18,0.34,0.20],[0.12,0.56,0.20,0.12],[0.31,0.13,0.37,0.19],[0.18,0.22,0.19,0.41]];
  return (
    <figure className="not-prose my-7 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm md:p-6"><p className="mb-2 font-bold text-slate-900">An attention head creates a token-to-token weight table</p><p className="mb-5 text-sm text-slate-600">Each row sums to 1. Darker cells receive more value information for that query token in this illustrative head.</p><div className="mx-auto grid max-w-md grid-cols-5 gap-1"><div />{["CLS","P1","P2","P3"].map(h=><div key={h} className="p-2 text-center text-xs font-bold text-slate-500">key {h}</div>)}{values.flatMap((row,r)=>[<div key={`r-${r}`} className="flex items-center justify-end pr-2 text-xs font-bold text-slate-500">query {["CLS","P1","P2","P3"][r]}</div>,...row.map((value,c)=><div key={`${r}-${c}`} className="flex aspect-square items-center justify-center rounded border border-indigo-200 text-xs font-bold text-indigo-950" style={{backgroundColor:`rgba(99,102,241,${0.12+value})`}}>{value.toFixed(2)}</div>)])}</div><figcaption className="mt-5 text-sm leading-relaxed text-slate-600">Different heads use different learned query, key and value projections, so they can model different relationships. Individual attention weights should not automatically be interpreted as faithful explanations of a model decision.</figcaption></figure>
  );
}

const comparisonRows = [
  ["Built-in assumption", "Local filters and shared weights", "Patch tokens and learned global attention"],
  ["Early information mixing", "Mostly local; receptive field grows with depth", "Any token can attend to any token in a standard ViT block"],
  ["Position", "Encoded naturally by the 2D convolution grid", "Added explicitly with position embeddings"],
  ["Data behaviour", "Strong local inductive bias can help with less data", "Plain ViT historically benefits strongly from large-scale pretraining"],
  ["Attention cost", "Convolution cost depends on kernel and feature-map sizes", "Global attention grows roughly with the square of token count"],
  ["Best practical choice", "Depends on model family, pretraining, task and hardware", "Depends on model family, pretraining, task and hardware"],
] as const;

export function VisionTransformersContent() {
  return (
    <div className="prose prose-lg max-w-none text-slate-700">
      <section className="not-prose mb-10 overflow-hidden rounded-3xl bg-gradient-to-br from-violet-950 via-indigo-900 to-sky-800 px-6 py-10 text-white shadow-xl md:px-10 md:py-12"><p className="mb-3 text-sm font-extrabold uppercase tracking-[0.18em] text-violet-200">Deep Learning · Computer Vision</p><h2 className="max-w-4xl text-3xl font-black leading-tight md:text-5xl">Vision Transformers: Reading an Image as a Sequence of Patches</h2><p className="mt-5 max-w-4xl text-lg leading-relaxed text-violet-50 md:text-xl">A Vision Transformer (ViT) replaces the CNN&apos;s sliding local filters with a Transformer encoder that relates image patches to one another. We will turn a picture into tokens, calculate the token count, explain class and position embeddings, perform one attention calculation, trace an encoder block, compare patch sizes, and use a pretrained ViT without claiming it always beats a CNN.</p></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">Why Convert an Image Into Tokens?</h2><p className="text-lg leading-relaxed">Imagine a satellite image containing a river, fields and a smoke plume. A CNN begins by applying small local filters; distant regions interact only after information crosses several layers. A Transformer offers another route: represent image regions as a sequence and let each region directly weigh information from every other region through <strong>self-attention</strong>.</p><p className="text-lg leading-relaxed">A Transformer was originally designed for sequences, not a height-by-width pixel grid. Passing one token per pixel would make the sequence enormous. ViT therefore groups nearby pixels into fixed, non-overlapping <strong>patches</strong>. Each patch is flattened and mapped to a fixed-length <strong>embedding</strong> vector. The result looks like a sequence of visual tokens.</p><PatchSequenceFigure /></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">Patch Size Controls the Sequence Length</h2><p className="text-lg leading-relaxed">For an H × W image and square P × P patches, the number of patch tokens is (H/P) × (W/P), assuming the dimensions divide evenly. The original ViT naming convention uses labels such as ViT-B/16: a Base-sized Transformer with 16 × 16 patches.</p><div className="not-prose rounded-2xl bg-slate-950 p-5 text-white md:p-6"><p className="text-xs font-extrabold uppercase tracking-[0.15em] text-cyan-300">Number of patch tokens</p><p className="mt-2 font-mono text-xl font-bold">N = (H × W) / P²</p><p className="mt-3 text-sm leading-relaxed text-slate-200">H and W are image height and width, P is patch side length, and N excludes any special class token.</p></div><div className="not-prose mt-5 rounded-2xl border border-indigo-200 bg-indigo-50 p-5 md:p-6"><h3 className="text-xl font-bold text-indigo-950">A 224 × 224 RGB image with 16 × 16 patches</h3><div className="mt-4 grid gap-3 md:grid-cols-3"><p className="rounded-lg bg-white p-4 font-mono text-sm leading-7"><strong>1. Patches per side</strong><br />224 / 16 = 14</p><p className="rounded-lg bg-white p-4 font-mono text-sm leading-7"><strong>2. Patch tokens</strong><br />14 × 14 = 196</p><p className="rounded-lg bg-white p-4 font-mono text-sm leading-7"><strong>3. Sequence length</strong><br />196 + 1 [CLS]<br />= <strong>197 tokens</strong></p></div><p className="mt-4 font-mono text-sm leading-7 text-slate-800">values in one RGB patch = 16 × 16 × 3 = <strong>768</strong></p><p className="mt-3 text-sm leading-relaxed text-slate-700">A learned linear projection converts those 768 raw values into the model&apos;s embedding dimension D. D also happens to be 768 in ViT-Base, but that equality is a model choice, not a general rule.</p></div></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">The Class Token Collects Evidence for Classification</h2><p className="text-lg leading-relaxed">ViT prepends one learned vector called <strong>[CLS]</strong> to the patch sequence. It contains no pixels. Across encoder layers, self-attention lets this token collect information from the patches. The final [CLS] representation is passed to a classifier that outputs image-level logits. Other architectures may pool patch tokens instead, so [CLS] is a design choice rather than a universal Transformer requirement.</p><PositionFigure /></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">Self-Attention Lets One Patch Request Context From Others</h2><p className="text-lg leading-relaxed">Each token is projected into three vectors. A <strong>query Q</strong> describes what this token is looking for. A <strong>key K</strong> describes what another token offers. Their dot product gives a compatibility score. A <strong>value V</strong> carries the information that will be mixed.</p><AttentionOriginsFigure /><div className="not-prose rounded-2xl bg-slate-950 p-5 text-white md:p-6"><p className="text-xs font-extrabold uppercase tracking-[0.15em] text-cyan-300">Scaled dot-product attention</p><p className="mt-2 overflow-x-auto whitespace-nowrap font-mono text-xl font-bold">Attention(Q,K,V) = softmax(QKᵀ / √d<sub>k</sub>)V</p><div className="mt-4 grid gap-3 text-sm text-slate-200 md:grid-cols-4"><p><strong className="text-white">QKᵀ:</strong> multiply each query by every key and add matching vector components to measure compatibility.</p><p><strong className="text-white">÷ √d<sub>k</sub>:</strong> scale scores before softmax so wider vectors do not create unnecessarily extreme dot products.</p><p><strong className="text-white">softmax:</strong> exponentiate and divide by the row total to create positive weights that sum to 1.</p><p><strong className="text-white">× V:</strong> multiply each value vector by its weight and add them to create the requested context.</p></div></div><div className="not-prose mt-5 rounded-2xl border border-violet-200 bg-violet-50 p-5 md:p-6"><h3 className="text-xl font-bold text-violet-950">One query attends to three patches</h3><p className="mt-2 text-slate-700">We manually choose two-number q, k and v vectors so the arithmetic remains visible. In a real forward pass, the learned projection matrices shown above calculate them. Here d<sub>k</sub>=2 because every query and key contains two numbers, so the scale is √2.</p><div className="mt-4 grid gap-3 md:grid-cols-2"><p className="rounded-lg bg-white p-4 font-mono text-sm leading-7"><strong>Inputs</strong><br />q=[1,0]<br />k₁=[1,0], k₂=[0,1], k₃=[1,1]<br />v₁=[2,0], v₂=[0,2], v₃=[1,1]</p><p className="rounded-lg bg-white p-4 font-mono text-sm leading-7"><strong>1. Dot products</strong><br />q·k₁=(1×1)+(0×0)=1<br />q·k₂=(1×0)+(0×1)=0<br />q·k₃=(1×1)+(0×1)=1</p><p className="rounded-lg bg-white p-4 font-mono text-sm leading-7"><strong>2. Scale the scores</strong><br />[1,0,1] / √2<br />≈ [0.707, 0, 0.707]</p><p className="rounded-lg bg-white p-4 font-mono text-sm leading-7"><strong>3. Softmax</strong><br />exp scores ≈ [2.028,1,2.028]<br />divide each by 5.056<br />≈ [0.401,0.198,0.401]</p><p className="rounded-lg bg-white p-4 font-mono text-sm leading-7 md:col-span-2"><strong>4. Weight and add the values</strong><br />0.401[2,0] + 0.198[0,2] + 0.401[1,1]<br />= [0.802,0] + [0,0.396] + [0.401,0.401]<br />= <strong>[1.203, 0.797]</strong></p></div><p className="mt-4 text-sm leading-relaxed text-slate-700"><strong>Interpretation:</strong> this query receives equal strong weight from patches 1 and 3, less from patch 2, then blends their value information. The two output numbers are the new context for this query in our tiny head.</p></div><AttentionMatrixFigure /></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">Multiple Heads and Encoder Blocks Build Richer Relationships</h2><p className="text-lg leading-relaxed"><strong>Multi-head attention</strong> runs several learned query/key/value projections in parallel. One head might become sensitive to colour continuity while another relates distant object parts; these interpretations are not guaranteed labels. Head outputs are concatenated and projected. A token-wise MLP then transforms each representation, while LayerNorm and residual shortcuts stabilize deep stacks.</p><div className="not-prose grid gap-3 rounded-2xl border border-indigo-200 bg-indigo-50 p-5 md:grid-cols-4"><p className="rounded-lg bg-white p-4 text-sm"><strong>Embedding width D = 768</strong><br /><span className="text-slate-600">chosen by the ViT-Base design</span></p><p className="rounded-lg bg-white p-4 text-sm"><strong>Heads h = 12</strong><br /><span className="text-slate-600">also an architecture choice</span></p><p className="rounded-lg bg-white p-4 text-sm"><strong>d<sub>k</sub> = D/h = 64</strong><br /><span className="text-slate-600">calculated width per head</span></p><p className="rounded-lg bg-white p-4 text-sm"><strong>12 × 64 = 768</strong><br /><span className="text-slate-600">concatenation restores D</span></p></div><EncoderBlockFigure /><p className="text-lg leading-relaxed">LayerNorm changes the distribution of values but keeps the token shape. Attention and the MLP also return width D so their outputs can be added to the residual path position by position. In ViT-Base, the MLP&apos;s hidden width 3,072 is a designer-selected expansion—four times 768—before a second learned layer returns it to 768.</p><p className="text-lg leading-relaxed">After one block, every patch token contains its original visual information plus context gathered from other tokens. Repeating blocks refines those representations. For classification, the final normalized [CLS] token is sent through a linear head to produce class logits.</p></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">Smaller Patches Preserve Detail but Make Attention Expensive</h2><p className="text-lg leading-relaxed">Standard global self-attention compares every token with every token, creating an N × N score matrix per head. Halving patch width gives four times as many 2D patches and roughly sixteen times as many token-pair scores.</p><div className="not-prose overflow-x-auto rounded-xl border border-slate-200 shadow-sm"><table className="w-full min-w-[760px] text-sm"><thead className="bg-slate-900 text-white"><tr><th className="p-3 text-left">Patch size on 224×224</th><th className="p-3 text-left">Patch tokens N</th><th className="p-3 text-left">Attention score pairs N²</th><th className="p-3 text-left">Trade-off</th></tr></thead><tbody className="divide-y divide-slate-100 bg-white">{[["32×32","49","2,401","Cheaper, but coarse patches can hide small details"],["16×16","196","38,416","Common balance for standard ViT"],["8×8","784","614,656","More local detail, much larger global-attention matrix"]].map(([p,n,n2,t])=><tr key={p}><td className="p-3 font-bold text-indigo-800">{p}</td><td className="p-3 font-mono">{n}</td><td className="p-3 font-mono">{n2}</td><td className="p-3 text-slate-700">{t}</td></tr>)}</tbody></table></div><p className="mt-4 text-sm leading-relaxed text-slate-600">The table omits [CLS], batch, head and embedding dimensions, but accurately shows the quadratic token relationship. Hierarchical/windowed designs such as Swin restrict attention regions to reduce cost for high-resolution vision.</p></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">CNN and ViT Encode Different Starting Assumptions</h2><div className="not-prose overflow-x-auto rounded-xl border border-slate-200 shadow-sm"><table className="w-full min-w-[940px] text-sm"><thead className="bg-slate-900 text-white"><tr><th className="p-3 text-left">Question</th><th className="p-3 text-left">Typical CNN</th><th className="p-3 text-left">Plain ViT</th></tr></thead><tbody className="divide-y divide-slate-100 bg-white">{comparisonRows.map(([q,cnn,vit])=><tr key={q} className="align-top"><td className="p-3 font-bold text-slate-900">{q}</td><td className="p-3 text-blue-800">{cnn}</td><td className="p-3 text-violet-800">{vit}</td></tr>)}</tbody></table></div><p className="mt-5 text-lg leading-relaxed">“ViT sees globally” does not mean it automatically understands every long-range relationship, and “CNN is local” does not mean later CNN layers cannot represent global context. Architecture, scale, pretraining, augmentation and optimization all matter. For a modest dataset, comparing pretrained models is more useful than repeating universal claims.</p></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">Use the Preprocessing Bundled With Pretrained Weights</h2><p className="text-lg leading-relaxed">A pretrained ViT expects the resolution, scaling and normalization used during its training. Torchvision weight objects provide matching transforms and class metadata, reducing silent preprocessing mismatch.</p><div className="not-prose overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div className="bg-slate-800 px-4 py-2 font-mono text-xs text-slate-300">vit_inference_and_finetuning.py</div><pre className="overflow-x-auto bg-[#1e1e1e] p-5 font-mono text-sm leading-relaxed text-[#d4d4d4]">{`import torch
from torch import nn
from torchvision.io import decode_image
from torchvision.models import vit_b_16, ViT_B_16_Weights

weights = ViT_B_16_Weights.DEFAULT
model = vit_b_16(weights=weights)
preprocess = weights.transforms()

# Pretrained inference on one image
image = decode_image("satellite_tile.jpg")
batch = preprocess(image).unsqueeze(0)
model.eval()
with torch.no_grad():
    logits = model(batch)
    class_id = int(logits.argmax(dim=1))
print(weights.meta["categories"][class_id])

# Adapt the classifier for four project classes.
input_features = model.heads.head.in_features
model.heads.head = nn.Linear(input_features, 4)

# A conservative start: train only the new head.
for parameter in model.parameters():
    parameter.requires_grad = False
for parameter in model.heads.parameters():
    parameter.requires_grad = True`}</pre></div><div className="not-prose mt-5 grid gap-3 md:grid-cols-2"><div className="rounded-xl border border-blue-200 bg-blue-50 p-4 text-sm leading-relaxed text-slate-700"><strong><code>ViT_B_16_Weights.DEFAULT</code></strong> selects an official pretrained weight recipe. <code>weights.transforms()</code> supplies the resize, crop, value scaling and normalization expected by those exact weights.</div><div className="rounded-xl border border-blue-200 bg-blue-50 p-4 text-sm leading-relaxed text-slate-700"><strong><code>unsqueeze(0)</code></strong> adds the batch dimension: one C × H × W image becomes 1 × C × H × W. The leading 1 is calculated from the one image being sent.</div><div className="rounded-xl border border-blue-200 bg-blue-50 p-4 text-sm leading-relaxed text-slate-700"><strong><code>model.eval()</code> and <code>torch.no_grad()</code></strong> switch inference-sensitive layers to evaluation behaviour and stop gradient recording. They do not alter the learned weights.</div><div className="rounded-xl border border-blue-200 bg-blue-50 p-4 text-sm leading-relaxed text-slate-700"><strong><code>nn.Linear(input_features, 4)</code></strong> replaces the original classifier. <code>input_features</code> is read from the model; 4 is chosen because this project has four labels. Its weights begin initialized and are learned during fine-tuning.</div></div><p className="mt-4 text-sm leading-relaxed text-slate-600">After the new head learns, optionally unfreeze later encoder blocks and fine-tune with a smaller learning rate. Freezing is not permanent: it is a staged starting strategy that should be validated.</p></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">Common Vision Transformer Problems</h2><div className="not-prose overflow-x-auto rounded-xl border border-slate-200 shadow-sm"><table className="w-full min-w-[960px] text-sm"><thead className="bg-slate-900 text-white"><tr><th className="p-3 text-left">Symptom</th><th className="p-3 text-left">Likely cause</th><th className="p-3 text-left">What to do</th></tr></thead><tbody className="divide-y divide-slate-100 bg-white">{[["Shape error at patch embedding","Image dimensions or channels do not match the model","Use the weight-specific transform; inspect [N,C,H,W] before the forward pass"],["Position-embedding size mismatch","Fine-tuning at a different resolution changes token count","Use a supported interpolation path or the expected resolution; do not discard learned positions blindly"],["Training from scratch performs poorly","Dataset too small or recipe too weak for a plain ViT","Begin from pretrained weights or compare a CNN with stronger local bias"],["Out of memory after smaller patches","N² attention matrix grew rapidly","Use larger patches, lower resolution/batch, checkpointing, or a windowed/hierarchical model"],["Attention map looks convincing but prediction is wrong","Attention weight is not a complete explanation","Use counterfactual/occlusion tests and quantitative evaluation alongside visualization"],["Fine-tuning destroys pretrained accuracy","Learning rate too large or all layers changed immediately","Train the head first, unfreeze gradually, and use discriminative/smaller rates"]].map(([s,c,a])=><tr key={s} className="align-top"><td className="p-3 font-bold text-slate-900">{s}</td><td className="p-3 text-rose-800">{c}</td><td className="p-3 text-indigo-800">{a}</td></tr>)}</tbody></table></div></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">When a ViT Is a Reasonable Starting Point</h2><div className="not-prose grid gap-4 md:grid-cols-2">{[["Strong pretrained checkpoint exists","A ViT can transfer rich patch relationships even when your labelled dataset is modest."],["Long-range image context matters","Global attention can connect separated regions directly, though task evidence must confirm the benefit."],["High-resolution dense prediction","Prefer a hierarchical/windowed vision Transformer or hybrid; plain global attention may be too expensive."],["Tiny edge deployment","Benchmark lightweight CNNs and compact/hybrid Transformers. Parameter count alone does not determine latency."],["Learning the basics","Implement a tiny patch embed and attention block, but do not expect from-scratch toy training to match a pretrained system."],["Need reliable explanation","Attention is not sufficient. Pair model choice with task-specific interpretability and validation methods."]].map(([title,body])=><div key={title} className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><p className="font-bold text-indigo-900">{title}</p><p className="mt-2 text-sm leading-relaxed text-slate-700">{body}</p></div>)}</div></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">The ViT in One Connected Explanation</h2><div className="not-prose rounded-2xl border border-violet-200 bg-violet-50 p-6"><ul className="m-0 space-y-2 pl-5 text-violet-950"><li>Split the image into patches and linearly project each flattened patch into an embedding.</li><li>Add position embeddings because attention alone does not know the 2D order.</li><li>Prepend [CLS] when using the classic ViT classification design.</li><li>Use multi-head self-attention so tokens exchange weighted value information.</li><li>Use MLPs, normalization and residual shortcuts inside repeated encoder blocks.</li><li>Smaller patches preserve detail but increase token count and quadratic global-attention cost.</li><li>Choose between CNNs, ViTs and hybrids through pretrained evidence on your own data and hardware.</li></ul></div></section>

      <section><h2 className="mb-4 border-b pb-2 text-2xl font-bold text-indigo-800">Continue Learning</h2><div className="not-prose mb-10 grid gap-4 md:grid-cols-2"><a href="/learn/object-detection" className="rounded-xl border border-slate-200 p-4 no-underline transition-colors hover:border-indigo-300 hover:bg-indigo-50"><p className="mb-1 text-xs font-bold uppercase text-indigo-600">Previous concept</p><p className="m-0 font-bold text-slate-900">Object Detection, Segmentation and Grad-CAM</p></a><a href="/learn/rnn-lstm" className="rounded-xl border border-slate-200 p-4 no-underline transition-colors hover:border-indigo-300 hover:bg-indigo-50"><p className="mb-1 text-xs font-bold uppercase text-indigo-600">Next concept</p><p className="m-0 font-bold text-slate-900">RNNs, LSTMs and GRUs</p></a></div></section>
    </div>
  );
}
