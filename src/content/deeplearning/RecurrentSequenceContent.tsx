import React from "react";

function SequenceFlowFigure() {
  const steps = [
    { t: "t = 1", x: "20°C", h: "h₁", note: "first reading" },
    { t: "t = 2", x: "24°C", h: "h₂", note: "uses h₁" },
    { t: "t = 3", x: "31°C", h: "h₃", note: "uses h₂" },
    { t: "t = 4", x: "38°C", h: "h₄", note: "alarm score" },
  ];
  return (
    <figure className="not-prose my-7 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">
      <div className="border-b border-slate-200 bg-slate-50 px-5 py-4"><p className="font-bold text-slate-900">An RNN reads a sequence one step at a time</p><p className="mt-1 text-sm text-slate-600">Each hidden state carries a learned summary of what has been read so far.</p></div>
      <div className="overflow-x-auto p-5 md:p-7"><div className="flex min-w-[800px] items-center gap-3">{steps.map((step, i) => <React.Fragment key={step.t}><div className="w-40 shrink-0 rounded-2xl border border-indigo-200 bg-indigo-50 p-4 text-center"><p className="text-xs font-bold uppercase tracking-wide text-indigo-600">{step.t}</p><p className="mt-2 rounded-lg bg-white px-3 py-2 font-mono font-bold text-slate-900">x{i + 1} = {step.x}</p><p className="my-2 text-lg font-black text-indigo-400">↓</p><p className="rounded-full bg-indigo-700 px-3 py-3 font-mono font-bold text-white">{step.h}</p><p className="mt-2 text-xs text-slate-600">{step.note}</p></div>{i < steps.length - 1 && <div className="flex-1 text-center"><p className="text-xs font-bold text-emerald-700">memory moves forward</p><p className="text-3xl font-black text-emerald-500">→</p></div>}</React.Fragment>)}</div></div>
      <figcaption className="border-t border-slate-100 px-5 py-3 text-sm leading-relaxed text-slate-600">The same RNN cell and the same weights are reused at every time step. The four boxes are an <strong>unrolled</strong> view of one repeated computation—not four unrelated networks.</figcaption>
    </figure>
  );
}

function SequenceTaskFigure() {
  const cards = [
    ["Many → one", "words → sentiment", "I / loved / the / film", "positive"],
    ["Many → many (aligned)", "readings → labels", "normal / normal / spike", "safe / safe / alert"],
    ["One → many", "image code → caption", "photo representation", "a / red / bus"],
    ["Many → many (shifted)", "past → future", "sales days 1…30", "forecast days 31…37"],
  ];
  return <figure className="not-prose my-7 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm md:p-6"><p className="font-bold text-slate-900">“Sequence model” describes several input–output shapes</p><div className="mt-4 grid gap-4 md:grid-cols-2">{cards.map(([kind, use, input, output]) => <div key={kind} className="rounded-xl border border-slate-200 bg-slate-50 p-4"><p className="text-xs font-extrabold uppercase tracking-wide text-indigo-700">{kind}</p><p className="mt-1 font-bold text-slate-900">{use}</p><div className="mt-3 flex items-center gap-2 text-xs"><span className="flex-1 rounded-lg bg-blue-100 p-2 text-blue-900">{input}</span><span className="font-black text-slate-400">→</span><span className="flex-1 rounded-lg bg-emerald-100 p-2 text-emerald-900">{output}</span></div></div>)}</div><figcaption className="mt-4 text-sm leading-relaxed text-slate-600">Choose the output shape from the task. Sentiment classification needs one prediction for the whole message; sequence labelling needs one prediction per meaningful step.</figcaption></figure>;
}

function GradientTimeFigure() {
  const values = [1, 0.5, 0.25, 0.125, 0.0625];
  return <figure className="not-prose my-7 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-200 bg-slate-50 px-5 py-4"><p className="font-bold text-slate-900">Repeated small derivatives can make an early learning signal disappear</p></div><div className="overflow-x-auto p-5 md:p-6"><svg viewBox="0 0 840 285" className="h-auto min-w-[720px] w-full" role="img" aria-labelledby="grad-time-title grad-time-desc"><title id="grad-time-title">Vanishing gradient across an unrolled recurrent network</title><desc id="grad-time-desc">A loss at time five sends a gradient backward. Multiplication by zero point five at each step reduces it from one to zero point zero six two five by time one.</desc><defs><marker id="grad-back-arrow" markerWidth="9" markerHeight="9" refX="8" refY="4.5" orient="auto"><path d="M0 0 L9 4.5 L0 9 Z" fill="#e11d48" /></marker></defs><rect width="840" height="285" rx="18" fill="#fff7ed" />{values.map((value, i) => { const x=90+i*155; return <g key={i}><circle cx={x} cy="120" r="43" fill="#e0e7ff" stroke="#4f46e5" strokeWidth="2" /><text x={x} y="115" textAnchor="middle" fontSize="18" fontWeight="800" fill="#312e81">h{i+1}</text><text x={x} y="140" textAnchor="middle" fontSize="12" fill="#475569">time {i+1}</text>{i<4&&<line x1={x+45} y1="120" x2={x+108} y2="120" stroke="#64748b" strokeWidth="3" />}</g>;})}<line x1="710" y1="205" x2="95" y2="205" stroke="#e11d48" strokeWidth="4" markerEnd="url(#grad-back-arrow)" /><text x="400" y="245" textAnchor="middle" fontSize="14" fontWeight="800" fill="#be123c">gradient travelling backward through time</text>{values.map((value, i) => <text key={i} x={90+i*155} y="188" textAnchor="middle" fontSize="13" fontWeight="800" fill="#be123c">{value}</text>)}</svg></div><figcaption className="border-t border-slate-100 px-5 py-3 text-sm leading-relaxed text-slate-600">This teaching example assumes a local derivative of 0.5 at each step: 1 × 0.5⁴ = 0.0625 by the earliest state. Values larger than 1 can instead make gradients grow rapidly. Real gradients are vectors and matrices, but repeated multiplication is the central difficulty.</figcaption></figure>;
}

function LSTMMemoryFigure() {
  return <figure className="not-prose my-7 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-200 bg-slate-50 px-5 py-4"><p className="font-bold text-slate-900">Inside one LSTM step: erase, write, then reveal</p><p className="mt-1 text-sm text-slate-600">The gates are learned numbers between 0 and 1 for every memory feature.</p></div><div className="overflow-x-auto p-4 md:p-6"><svg viewBox="0 0 900 390" className="h-auto min-w-[760px] w-full" role="img" aria-labelledby="lstm-title lstm-desc"><title id="lstm-title">LSTM cell state and three gates</title><desc id="lstm-desc">Previous hidden state and current input feed forget, input, candidate and output calculations. Old cell memory is filtered, new candidate memory is added, and the output gate reveals the new hidden state.</desc><defs><marker id="lstm-arrow" markerWidth="8" markerHeight="8" refX="7" refY="4" orient="auto"><path d="M0 0 L8 4 L0 8 Z" fill="#64748b" /></marker></defs><rect width="900" height="390" rx="18" fill="#f8fafc" /><text x="35" y="64" fontSize="14" fontWeight="800" fill="#334155">previous cell memory cₜ₋₁</text><line x1="215" y1="60" x2="700" y2="60" stroke="#16a34a" strokeWidth="6" markerEnd="url(#lstm-arrow)" /><rect x="275" y="38" width="54" height="44" rx="12" fill="#dcfce7" stroke="#16a34a" /><text x="302" y="66" textAnchor="middle" fontSize="20" fontWeight="900" fill="#166534">×</text><rect x="510" y="38" width="54" height="44" rx="12" fill="#dcfce7" stroke="#16a34a" /><text x="537" y="66" textAnchor="middle" fontSize="20" fontWeight="900" fill="#166534">+</text><text x="708" y="65" fontSize="14" fontWeight="800" fill="#166534">new cell memory cₜ</text>{[[160,"forget gate fₜ","how much old memory stays","#fee2e2","#dc2626"],[265,"input gate iₜ","how much new memory enters","#dbeafe","#2563eb"],[355,"candidate gₜ","proposed new content","#ede9fe","#7c3aed"]].map(([y,title,note,bg,border])=><g key={String(title)}><rect x="250" y={Number(y)-34} width="205" height="66" rx="14" fill={String(bg)} stroke={String(border)} strokeWidth="2" /><text x="352" y={Number(y)-7} textAnchor="middle" fontSize="14" fontWeight="800" fill="#0f172a">{title}</text><text x="352" y={Number(y)+14} textAnchor="middle" fontSize="11" fill="#475569">{note}</text></g>)}<rect x="555" y="231" width="205" height="66" rx="14" fill="#fef3c7" stroke="#d97706" strokeWidth="2" /><text x="657" y="258" textAnchor="middle" fontSize="14" fontWeight="800" fill="#0f172a">output gate oₜ</text><text x="657" y="279" textAnchor="middle" fontSize="11" fill="#475569">what becomes visible</text><rect x="35" y="237" width="150" height="78" rx="18" fill="#e2e8f0" stroke="#475569" strokeWidth="2" /><text x="110" y="268" textAnchor="middle" fontSize="15" fontWeight="800" fill="#0f172a">[hₜ₋₁, xₜ]</text><text x="110" y="292" textAnchor="middle" fontSize="11" fill="#475569">past summary + input</text><path d="M185 255 L245 168" fill="none" stroke="#64748b" strokeWidth="2" markerEnd="url(#lstm-arrow)" /><path d="M185 270 L245 263" fill="none" stroke="#64748b" strokeWidth="2" markerEnd="url(#lstm-arrow)" /><path d="M185 287 L245 350" fill="none" stroke="#64748b" strokeWidth="2" markerEnd="url(#lstm-arrow)" /><path d="M185 295 C360 380 500 330 552 280" fill="none" stroke="#64748b" strokeWidth="2" markerEnd="url(#lstm-arrow)" /><line x1="302" y1="126" x2="302" y2="85" stroke="#dc2626" strokeWidth="3" markerEnd="url(#lstm-arrow)" /><path d="M455 262 C485 220 500 130 530 84" fill="none" stroke="#2563eb" strokeWidth="3" markerEnd="url(#lstm-arrow)" /><path d="M455 350 C500 335 520 205 535 85" fill="none" stroke="#7c3aed" strokeWidth="3" markerEnd="url(#lstm-arrow)" /><line x1="735" y1="81" x2="735" y2="226" stroke="#16a34a" strokeWidth="3" markerEnd="url(#lstm-arrow)" /><circle cx="735" cy="335" r="43" fill="#fef3c7" stroke="#d97706" strokeWidth="2" /><text x="735" y="330" textAnchor="middle" fontSize="16" fontWeight="900" fill="#92400e">hₜ</text><text x="735" y="350" textAnchor="middle" fontSize="11" fill="#92400e">visible state</text><line x1="690" y1="298" x2="716" y2="306" stroke="#d97706" strokeWidth="3" markerEnd="url(#lstm-arrow)" /></svg></div><figcaption className="border-t border-slate-100 px-5 py-3 text-sm leading-relaxed text-slate-600">The upper green path is the cell state—a controlled memory route. Multiplication by fₜ filters old memory; adding iₜ × gₜ writes selected new content. The output gate controls the hidden state exposed to the next step and prediction layers.</figcaption></figure>;
}

const comparisonRows = [
  ["Simple RNN", "One hidden state; fewest gates/parameters", "Short dependencies, small teaching baselines", "Long-memory training can be difficult"],
  ["GRU", "Reset and update gates; no separate cell state", "A strong recurrent baseline when compute/data are limited", "Less explicit memory control than LSTM"],
  ["LSTM", "Forget, input and output gates plus cell state", "Forecasting, speech or text when gated recurrence is appropriate", "More parameters and sequential computation"],
  ["Transformer", "Attention connects token pairs directly", "Parallel training and rich global context", "Standard attention cost grows quadratically with length"],
] as const;

export function RecurrentSequenceContent() {
  return (
    <div className="prose prose-lg max-w-none text-slate-700">
      <section className="not-prose mb-10 overflow-hidden rounded-3xl bg-gradient-to-br from-slate-950 via-indigo-950 to-cyan-800 px-6 py-10 text-white shadow-xl md:px-10 md:py-12"><p className="mb-3 text-sm font-extrabold uppercase tracking-[0.18em] text-cyan-200">Deep Learning · Sequence Models</p><p className="mt-5 max-w-4xl text-lg leading-relaxed text-cyan-50 md:text-xl">Some data has an order that changes its meaning: temperatures rise over time, words form a sentence, and machine vibrations develop before a fault. This lesson builds a sequence model from the first hidden-state update, shows why ordinary RNN memory can fail, calculates an LSTM memory update, and compares GRUs and bidirectional models.</p></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">A Sequence Is More Than a Bag of Values</h2><p className="text-lg leading-relaxed">Suppose a factory motor reports 20°C, 24°C, 31°C and 38°C. The final value matters, but the rising pattern also warns us that the motor is heating quickly. If we shuffle the same readings to 38°C, 20°C, 31°C and 24°C, their average is unchanged while the story is completely different.</p><p className="text-lg leading-relaxed">A <strong>sequence model</strong> learns from both values and order. At time step t, it receives the current input x<sub>t</sub> and a compact summary of earlier inputs called the <strong>hidden state</strong> h<sub>t−1</sub>. It combines them to produce a new state h<sub>t</sub>. The state is learned—not a hand-written average—and its useful features emerge during training.</p><SequenceFlowFigure /><SequenceTaskFigure /></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">The Basic RNN Reuses One Update Rule</h2><p className="text-lg leading-relaxed">A recurrent cell uses the same parameters at every time step. W<sub>xh</sub> controls how the current input affects memory; W<sub>hh</sub> controls how previous memory affects new memory; b is a bias. The tanh activation compresses each state component between −1 and 1.</p><div className="not-prose rounded-2xl bg-slate-950 p-5 text-white md:p-6"><p className="text-xs font-extrabold uppercase tracking-[0.15em] text-cyan-300">Basic recurrent update</p><p className="mt-2 overflow-x-auto whitespace-nowrap font-mono text-xl font-bold">h<sub>t</sub> = tanh(W<sub>xh</sub>x<sub>t</sub> + W<sub>hh</sub>h<sub>t−1</sub> + b)</p><p className="mt-3 font-mono text-base">ŷ<sub>t</sub> = W<sub>hy</sub>h<sub>t</sub> + b<sub>y</sub></p><p className="mt-3 text-sm leading-relaxed text-slate-200">x<sub>t</sub> is the current input, h<sub>t−1</sub> is the previous hidden state, h<sub>t</sub> is the updated state, and ŷ<sub>t</sub> is an optional prediction. In a real network these are usually vectors and the W terms are matrices. Multiplication lets each learned weight strengthen, weaken or reverse a feature; addition combines current and remembered evidence; tanh keeps the resulting state in a bounded range.</p></div><div className="not-prose mt-5 rounded-2xl border border-indigo-200 bg-indigo-50 p-5 md:p-6"><h3 className="text-xl font-bold text-indigo-950">Two scalar updates, calculated by hand</h3><p className="mt-2 text-slate-700">Use W<sub>xh</sub>=0.5, W<sub>hh</sub>=0.8, b=0 and initial state h₀=0. Let the normalized inputs be x₁=1 and x₂=0.5. These five values are deliberately chosen teaching numbers. In a real run, x comes from the data, the zero initial state is created by the framework unless you provide another state, and training learns W and b.</p><div className="mt-4 grid gap-3 md:grid-cols-2"><div className="rounded-xl bg-white p-4"><p className="font-bold text-indigo-900">Time 1</p><p className="mt-2 font-mono text-sm leading-7">h₁ = tanh(0.5×1 + 0.8×0)<br />= tanh(0.5)<br />≈ <strong>0.462</strong></p><p className="mt-2 text-xs leading-relaxed text-slate-600">No earlier reading exists, so h₀ contributes zero.</p></div><div className="rounded-xl bg-white p-4"><p className="font-bold text-indigo-900">Time 2</p><p className="mt-2 font-mono text-sm leading-7">h₂ = tanh(0.5×0.5 + 0.8×0.462)<br />= tanh(0.620)<br />≈ <strong>0.551</strong></p><p className="mt-2 text-xs leading-relaxed text-slate-600">The same two weights are reused; only the input and state changed.</p></div></div><p className="mt-4 text-sm leading-relaxed text-slate-700"><strong>Interpretation:</strong> h₂ depends on x₂ and on the summary created from x₁. Without the recurrent 0.8×h₁ term, the second state would ignore the first reading.</p></div></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">Training Unrolls the Network Backward Through Time</h2><p className="text-lg leading-relaxed">During the forward pass, states move from early to late time steps. After a loss is calculated, <strong>backpropagation through time (BPTT)</strong> treats the unrolled sequence like a deep network with shared weights and sends gradient information from later states toward earlier ones.</p><p className="text-lg leading-relaxed">Each trip backward multiplies by another local derivative. Many factors below 1 can create a <strong>vanishing gradient</strong>, so early events receive almost no learning signal. Repeated large factors can create an <strong>exploding gradient</strong>, producing unstable updates or NaN values.</p><GradientTimeFigure /><div className="not-prose grid gap-4 md:grid-cols-2"><div className="rounded-xl border border-amber-200 bg-amber-50 p-5"><p className="font-bold text-amber-950">What gradient clipping does</p><p className="mt-2 text-sm leading-relaxed text-amber-900">If the total gradient norm is too large, clipping scales it down before the optimizer step. This protects against explosions; it does not restore information already lost to vanishing gradients.</p></div><div className="rounded-xl border border-violet-200 bg-violet-50 p-5"><p className="font-bold text-violet-950">What truncated BPTT does</p><p className="mt-2 text-sm leading-relaxed text-violet-900">For very long streams, detach the hidden state after a chosen window. This limits memory and compute, but prevents gradients from directly crossing the truncation boundary.</p></div></div></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">An LSTM Adds a Controlled Memory Path</h2><p className="text-lg leading-relaxed">A Long Short-Term Memory network is still recurrent, but its cell keeps two states. The <strong>cell state</strong> c carries controlled long-term memory. The <strong>hidden state</strong> h is the visible working representation passed to prediction layers and the next cell. Learned gates use sigmoid values between 0 and 1 to regulate information—not probabilities of an event, but soft control values.</p><LSTMMemoryFigure /><div className="not-prose rounded-2xl bg-slate-950 p-5 text-white md:p-6"><p className="text-xs font-extrabold uppercase tracking-[0.15em] text-cyan-300">One standard LSTM formulation</p><div className="mt-3 overflow-x-auto whitespace-nowrap font-mono text-sm leading-8"><p>f<sub>t</sub> = σ(W<sub>f</sub>[h<sub>t−1</sub>, x<sub>t</sub>] + b<sub>f</sub>) &nbsp; forget control</p><p>i<sub>t</sub> = σ(W<sub>i</sub>[h<sub>t−1</sub>, x<sub>t</sub>] + b<sub>i</sub>) &nbsp; input control</p><p>g<sub>t</sub> = tanh(W<sub>g</sub>[h<sub>t−1</sub>, x<sub>t</sub>] + b<sub>g</sub>) &nbsp; candidate</p><p>c<sub>t</sub> = f<sub>t</sub> ⊙ c<sub>t−1</sub> + i<sub>t</sub> ⊙ g<sub>t</sub> &nbsp; updated memory</p><p>o<sub>t</sub> = σ(W<sub>o</sub>[h<sub>t−1</sub>, x<sub>t</sub>] + b<sub>o</sub>) &nbsp; output control</p><p>h<sub>t</sub> = o<sub>t</sub> ⊙ tanh(c<sub>t</sub>) &nbsp; visible state</p></div><p className="mt-3 text-sm leading-relaxed text-slate-200">[h<sub>t−1</sub>,x<sub>t</sub>] means concatenate the two vectors so every gate can inspect past and present together. Matrix multiplication forms learned mixtures, σ turns each mixture into a 0–1 control, ⊙ applies that control feature by feature, and addition combines retained old memory with accepted new memory.</p></div><div className="not-prose mt-5 rounded-2xl border border-emerald-200 bg-emerald-50 p-5 md:p-6"><h3 className="text-xl font-bold text-emerald-950">One memory component, updated numerically</h3><p className="mt-2 text-slate-700">Suppose old memory c<sub>t−1</sub>=0.40. For this step the learned gates produce f<sub>t</sub>=0.80, i<sub>t</sub>=0.30, candidate g<sub>t</sub>=0.50 and output gate o<sub>t</sub>=0.90. These are manually selected teaching outputs, not recommended settings. In a trained LSTM, the current input and previous hidden state pass through learned W and b values to calculate a different gate vector at every step.</p><div className="mt-4 grid gap-3 md:grid-cols-3"><p className="rounded-lg bg-white p-4 font-mono text-sm leading-7"><strong>Retained old memory</strong><br />0.80 × 0.40<br />= 0.32</p><p className="rounded-lg bg-white p-4 font-mono text-sm leading-7"><strong>Accepted new memory</strong><br />0.30 × 0.50<br />= 0.15</p><p className="rounded-lg bg-white p-4 font-mono text-sm leading-7"><strong>New cell state</strong><br />c<sub>t</sub> = 0.32 + 0.15<br />= <strong>0.47</strong></p></div><p className="mt-3 rounded-lg bg-white p-4 font-mono text-sm leading-7">h<sub>t</sub> = 0.90 × tanh(0.47) ≈ 0.90 × 0.438 = <strong>0.394</strong></p><p className="mt-3 text-sm leading-relaxed text-slate-700"><strong>Interpretation:</strong> most of the old feature was preserved, some candidate information was written, and 90% of the squashed memory became visible. During training the network learns the weights that produce these gate values.</p></div></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">A GRU Combines the Memory Controls</h2><p className="text-lg leading-relaxed">A Gated Recurrent Unit has a hidden state but no separate cell state. Its <strong>reset gate</strong> controls how much previous state influences the candidate. Its <strong>update gate</strong> blends old state with the new candidate. Fewer gate projections usually mean fewer parameters than a comparable LSTM.</p><div className="not-prose rounded-2xl border border-violet-200 bg-violet-50 p-5 md:p-6"><p className="font-bold text-violet-950">The update gate performs a learned blend</p><p className="mt-3 overflow-x-auto whitespace-nowrap font-mono text-base text-violet-950">h<sub>t</sub> = (1 − z<sub>t</sub>) ⊙ n<sub>t</sub> + z<sub>t</sub> ⊙ h<sub>t−1</sub></p><p className="mt-3 text-sm leading-relaxed text-violet-900">In this convention, z near 1 preserves more old state and z near 0 selects more candidate state n. Some texts swap the two coefficients, so always read the implementation&apos;s equations.</p><p className="mt-3 rounded-lg bg-white p-4 font-mono text-sm">z=0.75, old h=0.60, candidate n=0.20<br />h<sub>t</sub>=(0.25×0.20)+(0.75×0.60)=0.05+0.45=<strong>0.50</strong></p></div><p className="mt-5 text-lg leading-relaxed">Do not choose LSTM because it is “always more powerful,” or GRU because it is “always faster.” Hidden size, implementation, hardware and task matter. Treat both as baselines and validate them on the same split and budget.</p></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">Bidirectional Models Use Past and Future Context</h2><p className="text-lg leading-relaxed">A bidirectional RNN runs one recurrent layer from left to right and another from right to left, then combines their representations. This is useful when the entire sequence is already available—for example, labelling every word in a stored document. The word “bank” can then use words on both sides.</p><div className="not-prose rounded-xl border-l-4 border-rose-500 bg-rose-50 p-5"><p className="font-bold text-rose-950">Do not use future information when it would be unavailable</p><p className="mt-2 text-sm leading-relaxed text-rose-900">A live forecasting system at 10:00 cannot use readings from 10:05. A bidirectional encoder on such training windows leaks the future and creates unrealistic evaluation. Use a causal, forward-only model for real-time prediction.</p></div></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">Which Sequence Model Should You Try?</h2><div className="not-prose overflow-x-auto rounded-xl border border-slate-200 shadow-sm"><table className="w-full min-w-[980px] text-sm"><thead className="bg-slate-900 text-white"><tr><th className="p-3 text-left">Model</th><th className="p-3 text-left">How it carries context</th><th className="p-3 text-left">Reasonable starting situation</th><th className="p-3 text-left">Important limitation</th></tr></thead><tbody className="divide-y divide-slate-100 bg-white">{comparisonRows.map(([model,context,use,limit])=><tr key={model} className="align-top"><td className="p-3 font-bold text-indigo-800">{model}</td><td className="p-3">{context}</td><td className="p-3 text-emerald-800">{use}</td><td className="p-3 text-rose-800">{limit}</td></tr>)}</tbody></table></div><p className="mt-5 text-lg leading-relaxed">For a small time-series or sequence-classification project, a one-layer GRU or LSTM is still an understandable, useful baseline. For large language modelling or parallel training, compare Transformer approaches. For extremely long streams, evaluate efficient attention or a supported SSM implementation—but measure quality, latency, memory and ecosystem maturity on your actual task.</p></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">A Small GRU Classifier in PyTorch</h2><p className="text-lg leading-relaxed">The model below receives a batch shaped [batch, time, features]. The GRU returns an output for every padded time position and a final hidden state. Packing tells the GRU which positions are real, so a short sequence does not treat padding as data.</p><div className="not-prose overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div className="bg-slate-800 px-4 py-2 font-mono text-xs text-slate-300">gru_sequence_classifier.py</div><pre className="overflow-x-auto bg-[#1e1e1e] p-5 font-mono text-sm leading-relaxed text-[#d4d4d4]">{`import torch
from torch import nn
from torch.nn.utils.rnn import pack_padded_sequence

class SequenceClassifier(nn.Module):
    def __init__(self, input_features, hidden_size, classes):
        super().__init__()
        self.gru = nn.GRU(
            input_size=input_features,
            hidden_size=hidden_size,
            batch_first=True,
        )
        self.classifier = nn.Linear(hidden_size, classes)

    def forward(self, padded_sequences, lengths):
        packed = pack_padded_sequence(
            padded_sequences,
            lengths.cpu(),
            batch_first=True,
            enforce_sorted=False,
        )
        _, final_hidden = self.gru(packed)
        last_layer_state = final_hidden[-1]   # [batch, hidden]
        return self.classifier(last_layer_state)

model = SequenceClassifier(input_features=6, hidden_size=32, classes=3)
logits = model(batch, lengths)  # raw class scores [batch, 3]
loss = nn.CrossEntropyLoss()(logits, labels)`}</pre></div><div className="not-prose mt-4 grid gap-3 md:grid-cols-2"><div className="rounded-xl border border-blue-200 bg-blue-50 p-4"><p className="font-bold text-blue-950">Where the dimensions come from</p><p className="mt-2 text-sm leading-relaxed text-blue-900"><code>input_features=6</code> must match the six measured or embedded features at each time step. <code>hidden_size=32</code> is a developer-chosen capacity. <code>classes=3</code> comes from the three target labels. With 10 examples and 20 time steps, the input shape is [10,20,6] and the logits shape is [10,3].</p></div><div className="rounded-xl border border-emerald-200 bg-emerald-50 p-4"><p className="font-bold text-emerald-950">What the framework does</p><p className="mt-2 text-sm leading-relaxed text-emerald-900">PyTorch initializes the GRU and linear weights, creates an all-zero initial hidden state when none is supplied, skips packed padding positions, and returns final states. CrossEntropyLoss internally applies log-softmax and selects the loss for each integer class label; pass raw logits, not pre-softmax probabilities.</p></div></div><p className="mt-4 text-sm leading-relaxed text-slate-600">For a bidirectional GRU, final_hidden contains a separate state for each direction; do not blindly take only [-1]. Reshape by layers and directions, then concatenate the last forward and backward states.</p></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">Diagnosing Recurrent Models</h2><div className="not-prose overflow-x-auto rounded-xl border border-slate-200 shadow-sm"><table className="w-full min-w-[980px] text-sm"><thead className="bg-slate-900 text-white"><tr><th className="p-3 text-left">What you observe</th><th className="p-3 text-left">Likely problem</th><th className="p-3 text-left">Check or correction</th></tr></thead><tbody className="divide-y divide-slate-100 bg-white">{[["Model reacts to padding length","Padding is entering pooling or final-state logic","Pack sequences or mask padded positions; test the same sample with extra padding"],["Loss becomes NaN; gradient norm jumps","Exploding gradients or unstable inputs/rate","Inspect inputs, lower the rate and clip gradient norm before optimizer.step()"],["Long-range clue has no effect","Vanishing gradients, short training window or inadequate state","Compare LSTM/GRU, longer context and a Transformer/SSM baseline"],["Validation is suspiciously excellent","Future leakage from bidirectionality or random temporal split","Use causal features and a chronological split for forecasting"],["Only one batch size works","Hidden state was created with a fixed batch dimension","Initialize from the current batch or let the framework create zero state"],["GPU memory grows every stream step","State retains the entire autograd graph","Detach state at intentional truncated-BPTT boundaries"],["Shapes look transposed","batch_first setting disagrees with data","Log [batch,time,features] or [time,batch,features] before the recurrent layer"]].map(([symptom,cause,fix])=><tr key={symptom} className="align-top"><td className="p-3 font-bold text-slate-900">{symptom}</td><td className="p-3 text-rose-800">{cause}</td><td className="p-3 text-indigo-800">{fix}</td></tr>)}</tbody></table></div></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">What to Remember</h2><div className="not-prose rounded-2xl border border-indigo-200 bg-indigo-50 p-6"><ul className="m-0 space-y-2 pl-5 text-indigo-950"><li>An RNN reuses one cell and carries a learned hidden state through ordered inputs.</li><li>BPTT sends gradients through the unrolled time steps; repeated derivatives can vanish or explode.</li><li>An LSTM regulates a separate cell memory with forget, input and output gates.</li><li>A GRU uses a simpler gated update with one hidden state; neither GRU nor LSTM wins every task.</li><li>Bidirectional context is useful only when future positions are available at prediction time.</li><li>Correct masking, causal evaluation, tensor shapes and gradient monitoring matter as much as the cell choice.</li></ul></div></section>

      <section><h2 className="mb-4 border-b pb-2 text-2xl font-bold text-indigo-800">Continue Learning</h2><div className="not-prose mb-10 grid gap-4 md:grid-cols-2"><a href="/learn/vision-transformers" className="rounded-xl border border-slate-200 p-4 no-underline transition-colors hover:border-indigo-300 hover:bg-indigo-50"><p className="mb-1 text-xs font-bold uppercase text-indigo-600">Previous concept</p><p className="m-0 font-bold text-slate-900">Vision Transformers</p></a><a href="/learn/attention-transformers-deep-learning" className="rounded-xl border border-slate-200 p-4 no-underline transition-colors hover:border-indigo-300 hover:bg-indigo-50"><p className="mb-1 text-xs font-bold uppercase text-indigo-600">Next concept</p><p className="m-0 font-bold text-slate-900">Sequence-to-Sequence Models and Attention</p></a></div></section>
    </div>
  );
}
