import React from "react";

function VisionTasksFigure() {
  const panelX = [15, 210, 405, 600];
  const titles = ["Classification", "Object detection", "Semantic segmentation", "Instance segmentation"];
  const details = ["one label for image", "class + box per object", "class for every pixel", "separate mask per object"];
  return (
    <figure className="not-prose my-7 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">
      <div className="border-b border-slate-200 bg-slate-50 px-5 py-4"><p className="font-bold text-slate-900">Four questions produce four different outputs</p><p className="mt-1 text-sm text-slate-600">The same street scene is represented differently according to the task.</p></div>
      <div className="overflow-x-auto p-3 md:p-6"><svg viewBox="0 0 790 310" className="h-auto min-w-[720px] w-full" role="img" aria-labelledby="vision-task-title vision-task-desc"><title id="vision-task-title">Comparison of image classification, object detection, semantic segmentation and instance segmentation</title><desc id="vision-task-desc">Four stylized street scenes show a whole image label, bounding boxes, one class color for all cars, and a different instance color for each car.</desc><rect width="790" height="310" rx="18" fill="#f8fafc" />{panelX.map((x, i) => <g key={titles[i]}><text x={x + 87} y="27" textAnchor="middle" fontSize="13" fontWeight="800" fill="#0f172a">{titles[i]}</text><rect x={x} y="45" width="175" height="165" rx="12" fill="#bfdbfe" stroke="#94a3b8" /><rect x={x} y="150" width="175" height="60" fill="#64748b" /><line x1={x + 10} y1="180" x2={x + 165} y2="180" stroke="white" strokeDasharray="16 10" strokeWidth="3" /><rect x={x + 18} y="125" width="70" height="42" rx="8" fill={i === 3 ? "#a78bfa" : "#f97316"} opacity={i >= 2 ? 0.72 : 1} /><circle cx={x + 33} cy="169" r="9" fill="#1e293b" /><circle cx={x + 74} cy="169" r="9" fill="#1e293b" /><rect x={x + 102} y="140" width="55" height="34" rx="7" fill={i === 3 ? "#34d399" : "#f97316"} opacity={i >= 2 ? 0.72 : 1} /><circle cx={x + 115} cy="176" r="8" fill="#1e293b" /><circle cx={x + 146} cy="176" r="8" fill="#1e293b" />{i === 0 && <g><rect x={x + 38} y="75" width="100" height="30" rx="15" fill="white" /><text x={x + 88} y="95" textAnchor="middle" fontSize="12" fontWeight="800" fill="#c2410c">cars present</text></g>}{i === 1 && <g><rect x={x + 10} y="116" width="87" height="69" fill="none" stroke="#dc2626" strokeWidth="3" /><rect x={x + 96} y="132" width="67" height="59" fill="none" stroke="#2563eb" strokeWidth="3" /></g>}{i === 2 && <g><path d={`M${x + 9} 116 H${x + 97} V185 H${x + 9} Z M${x + 96} 132 H${x + 164} V191 H${x + 96} Z`} fill="#fb923c" opacity="0.35" /></g>}{i === 3 && <g><path d={`M${x + 9} 116 H${x + 97} V185 H${x + 9} Z`} fill="#8b5cf6" opacity="0.3" /><path d={`M${x + 96} 132 H${x + 164} V191 H${x + 96} Z`} fill="#10b981" opacity="0.3" /></g>}<text x={x + 87} y="240" textAnchor="middle" fontSize="12" fontWeight="700" fill="#475569">{details[i]}</text></g>)}</svg></div>
      <figcaption className="border-t border-slate-100 px-5 py-3 text-sm leading-relaxed text-slate-600">Semantic segmentation labels both cars as the same class. Instance segmentation additionally tells us that they are two distinct cars. Panoptic segmentation combines instance-aware “things” with semantic background “stuff.”</figcaption>
    </figure>
  );
}

function IoUFigure() {
  return (
    <figure className="not-prose my-7 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">
      <div className="border-b border-slate-200 bg-slate-50 px-5 py-4"><p className="font-bold text-slate-900">Intersection over Union measures box overlap</p></div>
      <div className="grid items-center gap-5 p-5 md:grid-cols-[1fr_1.2fr] md:p-6"><svg viewBox="0 0 360 270" className="h-auto w-full" role="img" aria-label="Two overlapping bounding boxes with intersection highlighted"><rect width="360" height="270" rx="16" fill="#f8fafc" /><rect x="45" y="45" width="180" height="160" fill="#3b82f6" opacity="0.17" stroke="#2563eb" strokeWidth="4" /><text x="55" y="67" fontSize="13" fontWeight="800" fill="#1d4ed8">ground truth A</text><rect x="135" y="85" width="180" height="160" fill="#f97316" opacity="0.17" stroke="#ea580c" strokeWidth="4" /><text x="222" y="232" fontSize="13" fontWeight="800" fill="#c2410c">prediction B</text><rect x="135" y="85" width="90" height="120" fill="#7c3aed" opacity="0.5" /><text x="180" y="150" textAnchor="middle" fontSize="13" fontWeight="900" fill="#4c1d95">overlap</text></svg><div><div className="rounded-xl bg-slate-950 p-5 text-white"><p className="text-xs font-extrabold uppercase tracking-[0.15em] text-cyan-300">Intersection over Union</p><p className="mt-2 font-mono text-xl font-bold">IoU = area(A ∩ B) / area(A ∪ B)</p><p className="mt-3 text-sm leading-relaxed text-slate-200">The union includes every point covered by either box, counting the overlap once.</p></div><p className="mt-4 text-sm leading-relaxed text-slate-700">IoU = 1 means identical boxes; 0 means no overlap. A threshold such as 0.5 is a task/evaluation choice, not a universal definition of a correct detection.</p></div></div>
    </figure>
  );
}

function NmsFigure() {
  const steps = [
    ["1 · Rank", "A: 0.92, B: 0.78, C: 0.61"],
    ["2 · Keep A", "highest confidence"],
    ["3 · Compare", "IoU(A,B)=0.72; IoU(A,C)=0.08"],
    ["4 · Suppress B", "keep A and C"],
  ];
  return (
    <figure className="not-prose my-7 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm md:p-6"><p className="mb-5 font-bold text-slate-900">Non-maximum suppression turns duplicate proposals into cleaner detections</p><div className="grid items-center gap-3 md:grid-cols-[1fr_auto_1fr_auto_1fr_auto_1fr]">{steps.map(([title, detail], index) => <React.Fragment key={title}><div className={`rounded-xl border p-4 text-center ${index === 3 ? "border-emerald-200 bg-emerald-50" : "border-indigo-200 bg-indigo-50"}`}><p className="font-extrabold text-slate-900">{title}</p><p className="mt-2 text-xs leading-relaxed text-slate-600">{detail}</p></div>{index < steps.length - 1 && <span className="text-center text-xl font-black text-slate-400">→</span>}</React.Fragment>)}</div><figcaption className="mt-5 text-sm leading-relaxed text-slate-600">With an NMS IoU threshold of 0.5, B is removed because it overlaps A heavily and has a lower score. C remains because it likely describes another object. Crowded objects can challenge greedy NMS; soft-NMS reduces scores instead of immediately deleting boxes.</figcaption></figure>
  );
}

function UNetFigure() {
  const enc = [{ x: 25, y: 55, w: 85, h: 185, label: "128×128", ch: "features 32" }, { x: 150, y: 80, w: 85, h: 135, label: "64×64", ch: "features 64" }, { x: 275, y: 105, w: 85, h: 85, label: "32×32", ch: "features 128" }];
  const dec = [{ x: 440, y: 105, w: 85, h: 85, label: "32×32", ch: "decode" }, { x: 565, y: 80, w: 85, h: 135, label: "64×64", ch: "decode" }, { x: 690, y: 55, w: 85, h: 185, label: "128×128", ch: "pixel classes" }];
  return (
    <figure className="not-prose my-7 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-200 bg-slate-50 px-5 py-4"><p className="font-bold text-slate-900">A U-Net preserves detail while learning context</p></div><div className="overflow-x-auto p-3 md:p-6"><svg viewBox="0 0 800 320" className="h-auto min-w-[680px] w-full" role="img" aria-labelledby="unet-title unet-desc"><title id="unet-title">U-Net encoder, decoder and skip connections</title><desc id="unet-desc">An encoder reduces spatial resolution, a decoder restores resolution, and skip connections bring high-resolution encoder features to matching decoder stages.</desc><defs><marker id="unet-arrow" markerWidth="8" markerHeight="8" refX="7" refY="4" orient="auto"><path d="M0 0 L8 4 L0 8 Z" fill="#64748b" /></marker></defs><rect width="800" height="320" rx="18" fill="#f8fafc" /><text x="190" y="30" textAnchor="middle" fontSize="15" fontWeight="800" fill="#1d4ed8">encoder: what is present?</text><text x="615" y="30" textAnchor="middle" fontSize="15" fontWeight="800" fill="#7c3aed">decoder: where are its pixels?</text>{enc.map((b, i) => <g key={b.x}><rect x={b.x} y={b.y} width={b.w} height={b.h} rx="12" fill="#dbeafe" stroke="#2563eb" strokeWidth="2" /><text x={b.x + b.w/2} y={b.y+b.h/2-4} textAnchor="middle" fontSize="12" fontWeight="800" fill="#1e40af">{b.label}</text><text x={b.x+b.w/2} y={b.y+b.h/2+16} textAnchor="middle" fontSize="10" fill="#475569">{b.ch}</text>{i<enc.length-1&&<line x1={b.x+b.w} y1="150" x2={enc[i+1].x-8} y2="150" stroke="#64748b" strokeWidth="3" markerEnd="url(#unet-arrow)" />}</g>)}{dec.map((b,i)=><g key={b.x}><rect x={b.x} y={b.y} width={b.w} height={b.h} rx="12" fill="#ede9fe" stroke="#7c3aed" strokeWidth="2" /><text x={b.x+b.w/2} y={b.y+b.h/2-4} textAnchor="middle" fontSize="12" fontWeight="800" fill="#5b21b6">{b.label}</text><text x={b.x+b.w/2} y={b.y+b.h/2+16} textAnchor="middle" fontSize="10" fill="#475569">{b.ch}</text>{i<dec.length-1&&<line x1={b.x+b.w} y1="150" x2={dec[i+1].x-8} y2="150" stroke="#64748b" strokeWidth="3" markerEnd="url(#unet-arrow)" />}</g>)}<line x1="361" y1="150" x2="432" y2="150" stroke="#64748b" strokeWidth="3" markerEnd="url(#unet-arrow)" /><path d="M68 48 C120 280 660 280 732 248" fill="none" stroke="#10b981" strokeWidth="3" strokeDasharray="7 5" markerEnd="url(#unet-arrow)" /><path d="M193 74 C235 250 560 250 607 222" fill="none" stroke="#10b981" strokeWidth="3" strokeDasharray="7 5" markerEnd="url(#unet-arrow)" /><text x="400" y="293" textAnchor="middle" fontSize="13" fontWeight="800" fill="#047857">skip connections restore fine spatial detail</text></svg></div><figcaption className="border-t border-slate-100 px-5 py-3 text-sm leading-relaxed text-slate-600">The encoder sees larger context as resolution shrinks. The decoder upsamples. Skip connections concatenate corresponding encoder features so boundaries do not have to be reconstructed only from the smallest bottleneck.</figcaption></figure>
  );
}

function GradCamFigure() {
  return (
    <figure className="not-prose my-7 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm md:p-6"><p className="mb-5 font-bold text-slate-900">Grad-CAM asks which final feature-map regions supported one class score</p><div className="grid items-center gap-3 md:grid-cols-[1fr_auto_1fr_auto_1fr_auto_1fr]"><div className="relative h-32 overflow-hidden rounded-xl border border-slate-300 bg-slate-100"><div className="absolute inset-x-5 bottom-5 h-20 rounded-lg bg-slate-400" /><div className="absolute left-20 top-12 h-6 w-14 rounded bg-rose-500" /><p className="absolute left-2 top-2 rounded bg-white/90 px-2 py-1 text-xs font-bold">product image</p></div><span className="text-center text-xl font-black text-slate-400">→</span><div className="rounded-xl border border-blue-200 bg-blue-50 p-4 text-center"><p className="font-bold text-blue-900">class logit</p><p className="mt-2 font-mono text-sm">“defective”</p></div><span className="text-center text-xl font-black text-slate-400">→</span><div className="rounded-xl border border-violet-200 bg-violet-50 p-4 text-center"><p className="font-bold text-violet-900">gradients</p><p className="mt-2 text-xs text-slate-600">weight feature maps</p></div><span className="text-center text-xl font-black text-slate-400">→</span><div className="relative h-32 overflow-hidden rounded-xl border border-slate-300 bg-slate-100"><div className="absolute inset-x-5 bottom-5 h-20 rounded-lg bg-slate-400" /><div className="absolute left-12 top-8 h-20 w-24 rounded-full bg-red-500 opacity-45 blur-md" /><div className="absolute left-20 top-12 h-6 w-14 rounded bg-rose-600" /><p className="absolute left-2 top-2 rounded bg-white/90 px-2 py-1 text-xs font-bold">coarse heatmap</p></div></div><figcaption className="mt-5 text-sm leading-relaxed text-slate-600">A warm region means changing features there would strongly affect the selected class score. It does not prove causality, fairness or correct reasoning, and the final convolutional maps make the localization coarse.</figcaption></figure>
  );
}

const taskRows = [
  ["Image classification", "One or more labels for the whole image", "Product type, disease present/absent"],
  ["Object detection", "Class, confidence and bounding box for each object", "Count vehicles; locate safety equipment"],
  ["Semantic segmentation", "A class label for every pixel", "Road versus pavement; tumour region"],
  ["Instance segmentation", "Separate pixel mask for every object", "Outline and count touching cells"],
  ["Panoptic segmentation", "Semantic labels for all pixels plus separate countable instances", "Complete autonomous-driving scene map"],
] as const;

export function DetectionSegmentationContent() {
  return (
    <div className="prose prose-lg max-w-none text-slate-700">
      <section className="not-prose mb-10 overflow-hidden rounded-3xl bg-gradient-to-br from-rose-950 via-fuchsia-900 to-violet-800 px-6 py-10 text-white shadow-xl md:px-10 md:py-12"><p className="mb-3 text-sm font-extrabold uppercase tracking-[0.18em] text-rose-200">Deep Learning · Visual Understanding</p><p className="mt-5 max-w-4xl text-lg leading-relaxed text-rose-50 md:text-xl">Classification tells us what an image contains. Many real systems must also say where each object is, which pixels belong to it, or which regions influenced a prediction. This lesson separates those questions, calculates bounding-box overlap, explains duplicate removal, follows an encoder–decoder segmentation network, and computes a small Grad-CAM heatmap without treating it as proof.</p></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">Choose the Output Before Choosing the Model</h2><p className="text-lg leading-relaxed">A warehouse camera may need to answer “is a helmet visible?” That is classification. If it must count workers without helmets, it needs a location for each person: object detection. If a robot must grasp the exact visible shape of every package, boxes are too coarse and instance masks are more useful.</p><VisionTasksFigure /><div className="not-prose overflow-x-auto rounded-xl border border-slate-200 shadow-sm"><table className="w-full min-w-[850px] text-sm"><thead className="bg-slate-900 text-white"><tr><th className="p-3 text-left">Task</th><th className="p-3 text-left">Expected output</th><th className="p-3 text-left">Example use</th></tr></thead><tbody className="divide-y divide-slate-100 bg-white">{taskRows.map(([task, output, use]) => <tr key={task}><td className="p-3 font-bold text-indigo-800">{task}</td><td className="p-3 text-slate-700">{output}</td><td className="p-3 text-slate-700">{use}</td></tr>)}</tbody></table></div></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">A Detector Predicts What, Where and How Confident</h2><p className="text-lg leading-relaxed">A <strong>bounding box</strong> is commonly stored as its top-left and bottom-right coordinates: (x<sub>min</sub>, y<sub>min</sub>, x<sub>max</sub>, y<sub>max</sub>). The origin is usually the top-left of the image; x grows right and y grows down. A detector returns many candidate boxes, each with class scores and a confidence/objectness value.</p><div className="not-prose grid gap-4 md:grid-cols-3">{[["Class","What object might be here?","forklift"],["Box","Where is it?","[120, 45, 310, 220]"],["Score","How strongly did the model support it?","0.87"]].map(([name, question, value]) => <div key={name} className="rounded-xl border border-fuchsia-200 bg-fuchsia-50 p-5"><p className="text-xs font-bold uppercase text-fuchsia-700">{name}</p><p className="mt-2 font-bold text-slate-900">{question}</p><p className="mt-2 font-mono text-sm text-slate-700">{value}</p></div>)}</div><p className="mt-3 text-sm leading-relaxed text-slate-600">The box numbers are model-produced pixel coordinates: left x=120, top y=45, right x=310 and bottom y=220. The score 0.87 is also produced by the model. A developer chooses a score threshold that decides whether this candidate is shown; the threshold is not learned automatically by this output.</p><p className="mt-5 text-lg leading-relaxed">Training usually combines several losses. A classification term teaches the class, a localization term teaches box coordinates, and some detectors add objectness or mask losses. Their weights affect the trade-off; a low total number is meaningful only with the exact loss definition and validation metrics.</p><div className="not-prose rounded-2xl bg-slate-950 p-5 text-white"><p className="text-xs font-extrabold uppercase tracking-[0.15em] text-cyan-300">Simplified multi-task loss</p><p className="mt-2 overflow-x-auto whitespace-nowrap font-mono text-xl font-bold">L<sub>total</sub> = L<sub>class</sub> + λ<sub>box</sub>L<sub>box</sub> + λ<sub>obj</sub>L<sub>objectness</sub></p><p className="mt-3 text-sm text-slate-200">Each λ is a developer-selected hyperparameter that controls a term&apos;s relative contribution. It is multiplied by a loss because the raw terms can have different scales and priorities. The exact losses, weights and matching process differ across detector families.</p><div className="mt-4 grid gap-2 text-sm sm:grid-cols-4"><p className="rounded-lg bg-white/10 p-3">class loss<br /><strong>0.40</strong></p><p className="rounded-lg bg-white/10 p-3">box loss<br /><strong>0.20 × λ=5</strong></p><p className="rounded-lg bg-white/10 p-3">objectness<br /><strong>0.10 × λ=1</strong></p><p className="rounded-lg bg-cyan-400/20 p-3">total<br /><strong>0.40+1.00+0.10=1.50</strong></p></div><p className="mt-3 text-xs leading-relaxed text-slate-300">These small values and weights are manually chosen only to demonstrate the arithmetic. During real training, the model produces the three losses from its predictions and labels; the training configuration supplies the λ values.</p></div></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">Calculate Intersection over Union</h2><p className="text-lg leading-relaxed">A predicted box can name the right class but be badly placed. <strong>Intersection over Union (IoU)</strong> compares its overlap with a labelled ground-truth box while accounting for their combined area.</p><IoUFigure /><div className="not-prose rounded-2xl border border-indigo-200 bg-indigo-50 p-5 md:p-6"><h3 className="text-xl font-bold text-indigo-950">Worked box example</h3><p className="mt-2 text-slate-700">Ground truth A = (10, 10, 50, 50). Prediction B = (30, 20, 70, 60). Coordinates are (x<sub>min</sub>, y<sub>min</sub>, x<sub>max</sub>, y<sub>max</sub>).</p><div className="mt-4 grid gap-3 md:grid-cols-2 lg:grid-cols-4"><p className="rounded-lg bg-white p-4 font-mono text-sm leading-7"><strong>1. Areas</strong><br />A=(50−10)(50−10)=1600<br />B=(70−30)(60−20)=1600</p><p className="rounded-lg bg-white p-4 font-mono text-sm leading-7"><strong>2. Intersection</strong><br />width=min(50,70)−max(10,30)=20<br />height=30<br />area=600</p><p className="rounded-lg bg-white p-4 font-mono text-sm leading-7"><strong>3. Union</strong><br />1600+1600−600<br />=2600</p><p className="rounded-lg bg-white p-4 font-mono text-sm leading-7"><strong>4. IoU</strong><br />600/2600<br />≈ <strong>0.231</strong></p></div><p className="mt-4 text-sm leading-relaxed text-slate-700"><strong>Interpretation:</strong> only about 23.1% of the combined covered region overlaps. At IoU threshold 0.5 this prediction would not match the ground-truth object.</p></div></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">Why One Object Can Produce Several Boxes</h2><p className="text-lg leading-relaxed">Detectors examine many locations, scales or region proposals. Several candidates can surround the same car. <strong>Non-maximum suppression (NMS)</strong> is a post-processing step that keeps a high-scoring box and suppresses lower-scoring boxes of the same class that overlap it too strongly.</p><NmsFigure /><p className="text-lg leading-relaxed"><strong>One-stage detectors</strong> such as YOLO, SSD and RetinaNet predict dense boxes/classes in one main pass and are often chosen for speed. <strong>Two-stage detectors</strong> such as Faster R-CNN first generate promising regions, then classify/refine them; they can offer strong accuracy but often cost more latency. This is a family-level tendency, not a guarantee for every model and device.</p></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">Segmentation Predicts a Class at Pixel Resolution</h2><p className="text-lg leading-relaxed">A semantic segmentation model produces a C × H × W grid of logits: C class scores at every pixel location. C is chosen from the number of categories in the task; H and W are the output grid&apos;s height and width. During training, the target is an H × W grid of class IDs supplied by the labelled mask.</p><UNetFigure /><p className="text-sm leading-relaxed text-slate-600">In the teaching diagram, 128 → 64 → 32 is caused by two downsampling steps that each halve height and width. The channel counts 32 → 64 → 128 are designer-selected capacities, not spatial measurements. The decoder reverses the spatial changes, while skip connections copy same-resolution encoder features across so boundary detail is available again.</p><p className="text-lg leading-relaxed"><strong>U-Net</strong> uses an encoder to build context and a decoder to restore resolution. Same-scale skip connections recover fine detail. <strong>DeepLab</strong> families use atrous (dilated) convolution and multi-scale context so filters see wider regions without downsampling as aggressively. <strong>Mask R-CNN</strong> extends a detector with a mask branch for each detected instance.</p><div className="not-prose grid gap-4 md:grid-cols-2"><div className="rounded-xl bg-slate-950 p-5 text-white"><p className="text-xs font-bold uppercase tracking-wide text-cyan-300">Pixel IoU for one class</p><p className="mt-2 font-mono text-lg font-bold">IoU = TP / (TP + FP + FN)</p><p className="mt-3 text-sm text-slate-200">TP: correctly predicted class pixels; FP: extra predicted pixels; FN: missed class pixels.</p></div><div className="rounded-xl bg-slate-950 p-5 text-white"><p className="text-xs font-bold uppercase tracking-wide text-cyan-300">Dice coefficient</p><p className="mt-2 font-mono text-lg font-bold">Dice = 2TP / (2TP + FP + FN)</p><p className="mt-3 text-sm text-slate-200">Dice emphasizes overlap and ranges from 0 to 1 under this definition.</p></div></div><div className="not-prose mt-4 rounded-xl border border-emerald-200 bg-emerald-50 p-5 text-sm leading-relaxed"><strong>Tiny mask example:</strong> imagine the labelled object contains 80 pixels. The model correctly marks 60 of them (TP=60), misses 20 (FN=20), and incorrectly adds 10 background pixels (FP=10). IoU=60/(60+10+20)=0.667 and Dice=120/(120+10+20)=0.800. Both measure overlap, but Dice counts the correctly overlapping area twice.</div></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">Grad-CAM Explains a Class Score With a Coarse Heatmap</h2><p className="text-lg leading-relaxed">A classifier gives a label but no box. <strong>Grad-CAM</strong> uses gradients of one selected class score with respect to a convolutional feature tensor. It averages those gradients to assign one importance weight to each feature channel, combines the channel maps, and applies ReLU so positive support remains.</p><GradCamFigure /><div className="not-prose rounded-2xl bg-slate-950 p-5 text-white md:p-6"><p className="text-xs font-extrabold uppercase tracking-[0.15em] text-cyan-300">Grad-CAM for class c</p><p className="mt-2 overflow-x-auto whitespace-nowrap font-mono text-lg font-bold">α<sub>k</sub><sup>c</sup> = (1/Z) Σ<sub>i,j</sub> ∂y<sup>c</sup>/∂A<sub>i,j</sub><sup>k</sup> &nbsp;&nbsp; L<sup>c</sup> = ReLU(Σ<sub>k</sub> α<sub>k</sub><sup>c</sup>A<sup>k</sup>)</p><p className="mt-3 text-sm leading-relaxed text-slate-200"><strong>A<sup>k</sup></strong> is feature map k produced by the model, <strong>y<sup>c</sup></strong> is the selected class logit, <strong>Z</strong> is the calculated number of spatial cells, and α is that map&apos;s average sensitivity for class c. Division by Z turns a sum over locations into an average so channel weights remain comparable across the map.</p></div><div className="not-prose mt-5 rounded-2xl border border-violet-200 bg-violet-50 p-5 md:p-6"><h3 className="text-xl font-bold text-violet-950">Two 2 × 2 feature maps</h3><p className="mt-2 text-slate-700">We manually choose the two small feature maps so every cell is visible. Backpropagation would calculate four class-score gradients per map; averaging those four gradients produces α₁=0.5 and α₂=−0.25 for this teaching example.</p><p className="mt-3 overflow-x-auto font-mono text-sm leading-8 text-slate-800">A₁=[[1,2],[0,1]], A₂=[[0,1],[2,1]]<br />weight and add matching cells: 0.5A₁−0.25A₂<br />= [[0.50,0.75],[−0.50,0.25]]<br />keep positive support with ReLU: <strong>Lᶜ=[[0.50,0.75],[0,0.25]]</strong></p><p className="mt-3 text-sm leading-relaxed text-slate-700"><strong>Interpretation:</strong> the top-right cell provides the strongest positive support for class c in this coarse map. A negative combined cell becomes zero; Grad-CAM is showing supporting evidence, not a pixel-accurate object mask.</p></div></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">Evaluate the Whole Detection System</h2><p className="text-lg leading-relaxed">Detection evaluation must match predicted boxes to ground truth at an IoU threshold. <strong>Precision</strong> asks how many reported detections were correct; <strong>recall</strong> asks how many labelled objects were found. Varying the confidence threshold creates a precision–recall curve. <strong>Average Precision (AP)</strong> summarizes that curve for a class under a specified evaluation protocol; <strong>mAP</strong> averages AP across classes and sometimes across multiple IoU thresholds.</p><div className="not-prose overflow-x-auto rounded-xl border border-slate-200 shadow-sm"><table className="w-full min-w-[950px] text-sm"><thead className="bg-slate-900 text-white"><tr><th className="p-3 text-left">Failure</th><th className="p-3 text-left">What it changes</th><th className="p-3 text-left">What to inspect</th></tr></thead><tbody className="divide-y divide-slate-100 bg-white">{[["Correct class, poor box","IoU and localization AP fall","Box annotation consistency; small-object resolution; regression loss"],["Many duplicate boxes","Precision falls","NMS threshold, class-aware suppression and score calibration"],["Missed small objects","Recall and small-object AP fall","Input resolution, feature pyramid, label quality and cropping"],["Good masks, rough boundaries","Boundary quality weak despite acceptable region IoU","Annotation resolution, decoder detail, loss and post-processing"],["Grad-CAM highlights background","Possible shortcut learning","Background diversity, occlusion tests and counterfactual data—not heatmap alone"]].map(([failure, effect, inspect]) => <tr key={failure}><td className="p-3 font-bold text-slate-900">{failure}</td><td className="p-3 text-rose-800">{effect}</td><td className="p-3 text-indigo-800">{inspect}</td></tr>)}</tbody></table></div></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">Run a Pretrained Detector Before Training One</h2><p className="text-lg leading-relaxed">This PyTorch example loads both pretrained weights and their matching preprocessing. A detector expects a list of C × H × W image tensors and returns one dictionary per image.</p><div className="not-prose overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div className="bg-slate-800 px-4 py-2 font-mono text-xs text-slate-300">detect.py</div><pre className="overflow-x-auto bg-[#1e1e1e] p-5 font-mono text-sm leading-relaxed text-[#d4d4d4]">{`import torch
from torchvision.io import decode_image
from torchvision.models.detection import (
    fasterrcnn_resnet50_fpn_v2,
    FasterRCNN_ResNet50_FPN_V2_Weights,
)

weights = FasterRCNN_ResNet50_FPN_V2_Weights.DEFAULT
model = fasterrcnn_resnet50_fpn_v2(
    weights=weights, box_score_thresh=0.80
)
model.eval()

image = decode_image("warehouse.jpg")      # [C, H, W], uint8
input_image = weights.transforms()(image)   # matching preprocessing

with torch.no_grad():
    prediction = model([input_image])[0]

labels = [weights.meta["categories"][i] for i in prediction["labels"]]
for label, score, box in zip(labels, prediction["scores"], prediction["boxes"]):
    print(label, round(float(score), 3), box.tolist())

# Each box is [x_min, y_min, x_max, y_max].`}</pre></div><p className="mt-4 text-sm leading-relaxed text-slate-600">For custom training, each target must contain correctly typed boxes of shape [N,4], integer labels, and optional [N,H,W] masks. In the pretrained torchvision detection convention, class index 0 represents background. Transform boxes and masks with the image.</p></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">Choose the Simplest Output That Solves the Product Need</h2><div className="not-prose overflow-x-auto rounded-xl border border-slate-200 shadow-sm"><table className="w-full min-w-[920px] text-sm"><thead className="bg-slate-900 text-white"><tr><th className="p-3 text-left">Need</th><th className="p-3 text-left">Starting task/model direction</th><th className="p-3 text-left">Trade-off</th></tr></thead><tbody className="divide-y divide-slate-100 bg-white">{[["Say whether one condition exists","Classification + Grad-CAM for investigation","Cheapest labels; heatmap is not localization ground truth"],["Count and locate separate objects","Object detector","Box annotation costs more than image labels"],["Fast edge detection","Lightweight one-stage detector","Benchmark device latency and small-object recall"],["High-quality general detection baseline","Pretrained Faster R-CNN-family model","Often slower and heavier"],["Label every pixel by category","U-Net/DeepLab semantic segmentation","Pixel masks are expensive to annotate"],["Separate touching objects","Mask R-CNN or instance-segmentation model","More complex targets and evaluation"]].map(([need, direction, tradeoff]) => <tr key={need}><td className="p-3 font-bold text-slate-900">{need}</td><td className="p-3 font-bold text-indigo-800">{direction}</td><td className="p-3 text-slate-700">{tradeoff}</td></tr>)}</tbody></table></div></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">Limits You Must Not Hide</h2><div className="not-prose grid gap-4 md:grid-cols-2">{[["Annotation cost","Boxes are slower to label than classes; high-quality masks are slower still. Ambiguous boundaries create disagreement."],["Small and crowded objects","Downsampling removes detail, while NMS can suppress neighbouring instances. Report metrics by size and density."],["Domain shift","Weather, camera, hospital or production-line changes can break performance even when benchmark mAP is strong."],["Metric dependence","AP changes with IoU thresholds, class averaging, max detections and dataset protocol. State the exact metric."],["Heatmap over-trust","Grad-CAM is coarse and class-specific. Use it for investigation with other tests, not as proof of model reasoning."],["Real-time claims","Frames per second depends on resolution, batch size, preprocessing, post-processing, hardware and runtime. Measure end to end."]].map(([title, body]) => <div key={title} className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><p className="font-bold text-indigo-900">{title}</p><p className="mt-2 text-sm leading-relaxed text-slate-700">{body}</p></div>)}</div></section>

      <section><h2 className="mb-4 text-2xl font-bold text-indigo-800">The Connected Picture</h2><div className="not-prose rounded-2xl border border-fuchsia-200 bg-fuchsia-50 p-6"><ul className="m-0 space-y-2 pl-5 text-fuchsia-950"><li>Classification labels an image; detection adds boxes; segmentation assigns pixel labels; instance segmentation separates objects.</li><li>IoU measures geometric overlap and supports matching, NMS and evaluation.</li><li>NMS removes lower-scoring duplicate detections but needs careful thresholds in crowded scenes.</li><li>U-Net combines contextual encoding with detail-preserving skip connections.</li><li>Grad-CAM uses class gradients to weight feature maps and creates a coarse supporting-evidence map.</li><li>Choose the output and metric from the real decision the system must support.</li></ul></div></section>

      <section><h2 className="mb-4 border-b pb-2 text-2xl font-bold text-indigo-800">Continue Learning</h2><div className="not-prose mb-10 grid gap-4 md:grid-cols-2"><a href="/learn/cnn-architectures-resnet" className="rounded-xl border border-slate-200 p-4 no-underline transition-colors hover:border-indigo-300 hover:bg-indigo-50"><p className="mb-1 text-xs font-bold uppercase text-indigo-600">Previous concept</p><p className="m-0 font-bold text-slate-900">CNN Architectures: AlexNet to ResNet</p></a><a href="/learn/vision-transformers" className="rounded-xl border border-slate-200 p-4 no-underline transition-colors hover:border-indigo-300 hover:bg-indigo-50"><p className="mb-1 text-xs font-bold uppercase text-indigo-600">Next concept</p><p className="m-0 font-bold text-slate-900">Vision Transformers</p></a></div></section>
    </div>
  );
}
